# 6G AI Traffic Testbed Dependencies

# Network Emulation + Capture + PCAP analysis (local sibling package).
# netemu is a standalone package in the sibling netemu/ directory. It provides
# tc/netem shaping (netemu.emulator), tcpdump capture (netemu.capture), and
# pcap metric extraction (netemu.pcap). Install it with the [pcap] extra
# before installing these dependencies:
#   cd <repo-root>
#   pip install -e "netemu[pcap]"
#   pip install -r aitestbed/requirements.txt

# LLM Clients
# openai 3.x, anthropic 1.x and mcp 2.x switched to httpx2, which requires
# h11>=0.16. Every mitmproxy release that installs on Python 3.10/3.11 (<12)
# pins h11<0.15, and pip only finds that out after trying each of those
# releases, then gives up with "resolution-too-deep". The caps keep the
# resolver on the httpx-based lines the clients were written against.
openai>=1.0.0,<3
anthropic>=0.40.0,<1
azure-ai-inference>=1.0.0b1
google-genai>=1.0.0
websockets>=12.0  # For OpenAI Realtime API

# Data Processing
pandas>=2.0.0
pyarrow>=14.0.0
numpy>=1.24.0
openpyxl>=3.1.0  # Excel export backend for pandas.ExcelWriter

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0
requests>=2.28.0

# Logger
colorlog>=6.10.1

# Optional: Image processing for multimodal
Pillow>=10.0.0

# Optional: Token counting
tiktoken>=0.5.0

# L7 Capture (HTTP/HTTPS interception)
mitmproxy>=10.0.0

# L3/L4 capture + pcap analysis are provided by netemu[pcap] (see above).
# dpkt is pulled in by that extra; listed here only as a direct-use reminder
# for generate_charts.py, which parses pcaps with dpkt directly.
dpkt>=1.9.8

# Optional: WebRTC transport for realtime scenarios
# aiortc>=1.5.0 # use modified aiortc for realtime video analysis scenario

# Optional: Computer use executor
playwright>=1.41.0

# MCP (Model Context Protocol)
# Some MCP servers are npm packages, others are Python packages.
# Install npm-based servers: npm install -g @modelcontextprotocol/server-brave-search
# The Python MCP client uses subprocess to communicate with MCP servers.
mcp>=1.0.0,<2           # Python MCP client SDK (mcp.types, protocol constants); 2.x is httpx2-based, see "LLM Clients"
mcp-server-fetch>=2025.0.0

# Optional: Alpaca market data MCP server
# Install via: uvx alpaca-mcp-server init
# Or: pip install alpaca-mcp-server
alpaca-mcp-server>=0.1.0

# A2A (Agent2Agent protocol) scenarios
# Provides the a2a.client + a2a.server APIs used by clients/a2a_client.py and
# the local agents in a2a_agents/. Pin to the 1.x line the code targets.
a2a-sdk>=1.1.0,<2.0.0

# httpx — used directly by the A2A client/launcher and OpenClaw scenario.
# (Also pulled in transitively by openai/a2a-sdk; pinned here as a direct dep.)
httpx>=0.28.0

# uvicorn + starlette — serve the local A2A agents (a2a_agents/common.py)
uvicorn>=0.27.0
starlette>=0.40.0

# Async support (included in Python 3.7+, but useful for type hints)
# asyncio is part of stdlib

# VLM
torch==2.4.1 
torchvision==0.19.1
transformers==4.44.2
